# GoReleaser configuration
# Documentation: https://goreleaser.com

version: 2

project_name: md-go-validator

before:
  hooks:
    - go mod tidy

builds:
  - id: md-go-validator
    main: .
    binary: md-go-validator
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: darwin
        goarch: 386
      - goos: windows
        goarch: arm64
    tags:
      - netgo
      - osusergo
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{- title .Os }}_{{- if eq .Arch "amd64" }}x86_64
        {{- else if eq .Arch "386" }}i386
        {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE*
      - README*
      - CHANGELOG*

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

signs:
  - cmd: cosign
    certificate: "${artifact}.pem"
    args:
      - sign-blob
      - "--oidc-issuer=https://token.actions.githubusercontent.com"
      - "--output-certificate=${certificate}"
      - "--output-signature=${signature}"
      - "${artifact}"
    artifacts: all
    output: true

sboms:
  - artifacts: archive

brews:
  - name: md-go-validator
    skip_upload: auto
    homepage: "https://github.com/LarsArtmann/md-go-validator"
    description: "Markdown validation tool for Go projects"
    license: MIT
    repository:
      owner: LarsArtmann
      name: homebrew-tap
    directory: Formula
    commit_author:
      name: goreleaserbot
      email: goreleaser@larsartmann.com
    install: |
      bin.install "md-go-validator"
    test: |
      system "#{{bin}}/md-go-validator --version"

nix:
  - name: md-go-validator
    skip_upload: auto
    repository:
      owner: LarsArtmann
      name: nur-packages
    homepage: "https://github.com/LarsArtmann/md-go-validator"
    description: "Markdown validation tool for Go projects"
    license: mit
    formatter: nixfmt
    path: pkgs/md-go-validator/default.nix
    install: |
      mkdir -p $out/bin
      cp -vr ./{{binary}} $out/bin/md-go-validator

nfpms:
  - id: default
    package_name: md-go-validator
    file_name_template: "{{ .ConventionalFileName }}"
    homepage: "https://github.com/LarsArtmann/md-go-validator"
    description: "Markdown validation tool for Go projects"
    license: MIT
    maintainer: "Lars Artmann <lars@larsartmann.com>"
    formats:
      - deb
      - rpm
      - apk

scoops:
  - name: md-go-validator
    skip_upload: auto
    repository:
      owner: LarsArtmann
      name: homebrew-tap
    directory: Scoop
    commit_author:
      name: goreleaserbot
      email: goreleaser@larsartmann.com
    homepage: "https://github.com/LarsArtmann/md-go-validator"
    description: "Markdown validation tool for Go projects"
    license: MIT

release:
  github:
    owner: LarsArtmann
    name: md-go-validator
  draft: false
  prerelease: auto
  name_template: "md-go-validator v{{.Version}}"
