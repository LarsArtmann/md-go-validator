# Status Report — 2026-06-13 12:06 CEST

_Generated after upgrading go-output to v0.10.0, fixing the nix vendor hash, and creating a forward-looking execution plan._

---

## Executive Summary

| Dimension        | Status                                                       |
| ---------------- | ------------------------------------------------------------ |
| **Build**        | PASSING (`go build`, `go test -race`, `golangci-lint` green) |
| **Nix**          | PASSING (`nix flake check` all checks green)                 |
| **Tests**        | ALL GREEN — 7/7 packages, 0 failures, race-clean             |
| **Lint**         | 0 issues (golangci-lint with 90+ linters)                    |
| **Coverage**     | 70.9%–93.8% across packages (cmd still lowest)               |
| **Codebase**     | ~7,400+ lines of Go (production + tests)                     |
| **Last release** | v0.1.0 (2026-01-01) — 6 months of unreleased changes         |
| **Dependencies** | go-output v0.10.0, gotreesitter v0.20.2, Go 1.26.3           |
| **CI**           | Minimal — test/lint/build on push/PR to master               |
| **Go workspace** | CLEAN — no `go.work` file, no local replace overrides        |

---

## a) FULLY DONE

### This Session's Work

| What                                  | Files Changed      | Impact                                                     |
| ------------------------------------- | ------------------ | ---------------------------------------------------------- |
| Upgrade `go-output` family to v0.10.0 | `go.mod`, `go.sum` | Latest API, fixes stale local-workaround dependency state  |
| Refresh all transitive dependencies   | `go.mod`, `go.sum` | `go get -u ./...` + `go mod tidy`                          |
| Fix nix `vendorHash` mismatch         | `flake.nix`        | `nix flake check` now passes; reproducible builds restored |
| Verify build/test/lint/nix            | whole repo         | All quality gates green                                    |
| Update `AGENTS.md` dependency notes   | `AGENTS.md`        | Removed stale nix-breakage note, updated go-output version |

### Core Library (pkg/) — Still Complete

| Component                           | File(s)                                                             | Description                                                                                                                                     |
| ----------------------------------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| **Go code validation**              | `pkg/languages/go_validator.go`                                     | 5-strategy parser (complete file → package wrap → func wrap → expression → statements). Handles partial snippets from documentation.            |
| **Multi-language support**          | `pkg/languages/treesitter_validator.go`                             | 8 languages via tree-sitter: Go, Templ, TypeScript, TSX, Rust, Nix, HCL, Terraform                                                              |
| **Language registry**               | `pkg/languages/validator.go`, `language.go`                         | `Registry` with `Register()`, `Get()`, `GetByString()`, `GetAvailable()`. Pluggable validator interface.                                        |
| **Code block extraction**           | `pkg/extractor.go`                                                  | Line-by-line markdown parser for fenced blocks with language filtering. MDX support.                                                            |
| **Skip directives**                 | `pkg/extractor.go`                                                  | Configurable: `<!-- skip-validate -->`, `<!-- skip-md-validate -->`, `<!-- md-skip -->`, `<!-- no-validate -->`, `// skip-validate`, `//nolint` |
| **Concurrent directory validation** | `pkg/validator.go`                                                  | Worker pool with channels, context cancellation, configurable concurrency                                                                       |
| **Context lifecycle**               | `pkg/context.go`                                                    | Timeout, deadline, max files/blocks, parent propagation, branching for parallel workers                                                         |
| **Multi-format output**             | `pkg/output/output.go`                                              | 6 formats: Table, JSON, YAML, CSV, Markdown, Quiet. Via go-output library.                                                                      |
| **Branded types**                   | `pkg/types/`                                                        | `FileID`, `LineNumber`, `BlockIndex`, `FileType`, `ValidationStatus`, `Language`. Compile-time safety.                                          |
| **Code utilities**                  | `pkg/code/util.go`                                                  | `IndentCode()`, `ParseGo()`, `TruncateForError()`                                                                                               |
| **Test helpers**                    | `pkg/testutil/`, `pkg/types/testing.go`, `pkg/languages/testing.go` | Shared assertion helpers across packages                                                                                                        |

### CLI (cmd/)

- Manual generics-based argument parser with `--verbose`, `--quiet`, `--format`, `--color`, `--output`, `--timeout`, `--languages`, multi-path support
- Custom usage/help header

### Infrastructure

- **Nix flake** — flake-parts + treefmt-nix. Build, test, format, devShell, overlay.
- **GoReleaser** — Cross-compile (linux/darwin/windows, amd64/arm64), cosign signing, SBOMs, brew/scoop/nix/nfpm.
- **CI (GitHub Actions)** — Test (with race), lint (golangci-lint), build.
- **Linter config** — 90+ linters, Go 1.26.3, strict settings.

---

## b) PARTIALLY DONE

| Item                              | State | Details                                                                                           |
| --------------------------------- | ----- | ------------------------------------------------------------------------------------------------- |
| `README.md`                       | 90%   | Polished, covers CLI + library. Could mention new `--version` once implemented.                   |
| `CHANGELOG.md`                    | 80%   | Unreleased section is substantial — no version bump since v0.1.0 in January                       |
| `CONSUMER_PERSPECTIVE.md`         | 70%   | 15 gaps identified, ~2 addressed (`--version` not yet done)                                       |
| `docs/modularization/PROPOSAL.md` | 50%   | Dead dependency cycle `pkg/types` ↔ `pkg/languages` identified, proposal written but not executed |
| Nix build                         | 95%   | `nix flake check` passes; overlay still references missing `package.nix` (latent bug)             |

---

## c) NOT STARTED

### Consumer-Facing Gaps (from `CONSUMER_PERSPECTIVE.md`):

| #   | Gap                                                    | Severity |
| --- | ------------------------------------------------------ | -------- |
| 1   | `--version` flag                                       | Critical |
| 2   | Configuration file support (`.md-go-validator.yaml`)   | Critical |
| 3   | `--init` command for config generation                 | Critical |
| 4   | `.md-go-validator-ignore` / exclude patterns           | Critical |
| 5   | Fix `CONTRIBUTING.md` dead references                  | Critical |
| 6   | Reusable GitHub Action (`action.yml`)                  | Major    |
| 7   | Pre-commit hook integration (`.pre-commit-hooks.yaml`) | Major    |
| 8   | Watch / incremental mode (`--watch`)                   | Major    |
| 9   | Error codes in CLI output                              | Major    |
| 10  | Diff / regression mode (`--baseline`)                  | Major    |
| 11  | `--dry-run` flag                                       | Moderate |
| 12  | Progress indicator                                     | Moderate |
| 13  | Granular exit codes (errors vs crash vs no files)      | Moderate |
| 14  | Self-validation in CI (dogfooding)                     | Moderate |
| 15  | `--fail-on-skipped` option                             | Minor    |

### Project Hygiene

- **`TODO_LIST.md`** — Does not exist
- **`FEATURES.md`** — Does not exist
- **`ROADMAP.md`** — Does not exist
- **`CHANGELOG.md` version bump** — v0.1.0 was 2026-01-01, 6 months of unreleased work

### Architecture / Type Model Improvements

- No first-class `Config` domain type; CLI builds an ad-hoc `config` struct manually
- `pkg/types` ↔ `pkg/languages` import cycle remains (`Language` type lives in `languages`)
- `Result` is a single struct with optional `Error`; could be a sum type
- No filesystem abstraction; tests hit real `os` calls
- No structured error classification (rejected vs transient vs input vs internal)

---

## d) TOTALLY FUCKED UP

| Issue                                                | Severity | Details                                                                                                                                                            |
| ---------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **6 months without a release**                       | HIGH     | v0.1.0 was 2026-01-01. Massive unreleased changes: MDX support, tree-sitter, branded types, multi-format output, context support, skip directives overhaul.        |
| **`CONTRIBUTING.md` is broken**                      | MEDIUM   | References `just` commands and setup scripts that don't exist. First contributor experience is broken.                                                             |
| **No dogfooding in CI**                              | MEDIUM   | Tool validates markdown code blocks but doesn't validate its own docs in CI. Undermines credibility.                                                               |
| **`flake.overlays.default` references missing file** | MEDIUM   | `flake.nix:132` calls `./package.nix`, which does not exist. `nix flake check` doesn't evaluate overlays, so this latent bug hides until someone uses the overlay. |
| **Compiled binary in working tree**                  | LOW      | `md-go-validator` binary present (not tracked). Risk of accidental commit. Should be gitignored/removed.                                                           |

---

## e) WHAT WE SHOULD IMPROVE

### 1. What I Forgot / Could Have Done Better

1. **Did not run `nix flake check` immediately** — I stopped at `go build`, `go test`, and `golangci-lint`. The nix `vendorHash` mismatch only surfaced later. Nix is a first-class build path in this repo and must be verified on any dependency change.
2. **Did not check `go-output` v0.10.0 deprecations proactively** — Build passes, but I did not audit whether we still call deprecated APIs (`MarshalTSV`, old registry helpers, etc.). We should migrate away from deprecated symbols in the next pass.
3. **Did not update `CHANGELOG.md`** — A dependency upgrade that fixes a nix build deserves a changelog entry.
4. **Did not add `--version` while touching release-adjacent code** — The binary already receives a version via ldflags in nix/goreleaser, but the CLI cannot print it.
5. **Did not remove the stray `md-go-validator` binary** — Pre-commit hook flagged it. Working-tree cleanliness matters.

### 2. Architecture & Type Model Improvements

| Opportunity                     | Current State                                                                 | Proposed Improvement                                                                         |
| ------------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| **Config as a domain type**     | `cmd/md-go-validator/main.go` has an unexported `config` struct built by hand | Create `pkg/config.Config` with validated fields, defaults, and file/env loading             |
| **Result sum type**             | `types.Result` is one struct with optional `Error`                            | Introduce `ValidResult`, `SkippedResult`, `ErrorResult` with a shared interface              |
| **Break types↔languages cycle** | `types.CodeBlock` imports `languages.Language`                                | Move `Language` (the type) to `pkg/types` or a new `pkg/language` package                    |
| **Filesystem abstraction**      | Direct `os` usage in `pkg/validator.go` and CLI                               | Use `io/fs.FS` or `github.com/spf13/afero` for testability and virtual filesystem support    |
| **Structured errors**           | Plain `fmt.Errorf` wrapping                                                   | Adopt `github.com/larsartmann/go-error-family` for rejection/transient/input classifications |
| **Status enum encoding**        | `ValidationStatus` is already a branded enum                                  | Good; keep and extend to error-code enum                                                     |

### 3. Use Well-Established Libraries

| Area                | Current Approach                                 | Established Alternative                             | Benefit                                                                 |
| ------------------- | ------------------------------------------------ | --------------------------------------------------- | ----------------------------------------------------------------------- |
| CLI parsing         | Hand-rolled generics parser                      | `cobra` + `viper`                                   | `--version`, config files, env vars, shell completion, sub-commands     |
| Configuration       | None                                             | `viper` or `koanf`                                  | `.md-go-validator.yaml`, env overrides, defaults, validation            |
| Filesystem tests    | Real `os` calls                                  | `afero`                                             | Fast, deterministic, parallel-safe tests; virtual paths for `--dry-run` |
| Syntax highlighting | Hardcoded ANSI escapes in `pkg/output/output.go` | `chroma` or `lipgloss` (already in go-output)       | Maintainable, themeable, correct code rendering in terminal output      |
| Structured logging  | `fmt` to stdout/stderr                           | `slog` or `zerolog`                                 | JSON logs in CI, leveled output, observability                          |
| Error handling      | Plain errors                                     | `go-error-family`                                   | Classified errors, retry hints, clean user-facing messages              |
| Ignore patterns     | None                                             | `github.com/bmatcuk/doublestar` or `ignore` package | `.gitignore`-style exclude patterns                                     |
| Progress UI         | None                                             | `charmbracelet/bubbletea` or `cheggaaa/pb`          | Progress bars for large directories                                     |

---

## f) Top 25 Things We Should Get Done Next

### Tier 1: Critical (Adoption Blockers)

| #   | Task                                                              | Impact                                    | Effort |
| --- | ----------------------------------------------------------------- | ----------------------------------------- | ------ |
| 1   | **Cut v0.2.0 release**                                            | Ships 6 months of work to users           | Small  |
| 2   | **Add `--version` flag**                                          | Users can verify installation             | Small  |
| 3   | **Fix `flake.overlays.default`** (create or remove `package.nix`) | Overlay consumers don't get a broken path | Small  |
| 4   | **Fix `CONTRIBUTING.md` dead references**                         | Contributors don't hit dead ends          | Small  |
| 5   | **Add self-validation to CI**                                     | Dogfooding builds trust                   | Small  |

### Tier 2: High Impact (Quality of Life)

| #   | Task                                                     | Impact                                | Effort |
| --- | -------------------------------------------------------- | ------------------------------------- | ------ |
| 6   | **Configuration file support** (`.md-go-validator.yaml`) | Users commit settings to repo         | Medium |
| 7   | **GitHub Action** (`action.yml`)                         | Single-line CI integration            | Medium |
| 8   | **Exclude patterns** (`.md-go-validator-ignore`)         | Skip vendor/generated files           | Small  |
| 9   | **`--init` command**                                     | Generate starter config               | Small  |
| 10  | **Pre-commit hooks** (`.pre-commit-hooks.yaml`)          | Ecosystem discoverability             | Small  |
| 11  | **Use `go-error-family` for structured errors**          | Classified, actionable error messages | Medium |

### Tier 3: Important (Polish)

| #   | Task                                                             | Impact                           | Effort |
| --- | ---------------------------------------------------------------- | -------------------------------- | ------ |
| 12  | **Granular exit codes** (0=valid, 1=errors, 2=crash, 3=no files) | CI can distinguish failure modes | Small  |
| 13  | **Error codes in JSON output**                                   | Machine-actionable results       | Small  |
| 14  | **Create `TODO_LIST.md`**                                        | Prioritized backlog              | Small  |
| 15  | **Create `FEATURES.md`**                                         | Feature inventory                | Small  |
| 16  | **Increase cmd coverage to 85%+**                                | Confidence in CLI edge cases     | Medium |
| 17  | **Break types ↔ languages dependency cycle**                     | Clean architecture               | Medium |
| 18  | **Add `internal/` package boundary**                             | Enforce encapsulation            | Medium |

### Tier 4: Nice to Have (Enhancement)

| #   | Task                                      | Impact                          | Effort |
| --- | ----------------------------------------- | ------------------------------- | ------ |
| 19  | **Watch mode (`--watch`)**                | Development workflow            | Medium |
| 20  | **Progress indicator**                    | UX for large directories        | Small  |
| 21  | **`--dry-run` flag**                      | Debug config without running    | Small  |
| 22  | **Diff/regression mode (`--baseline`)**   | Incremental adoption            | Large  |
| 23  | **`--fail-on-skipped` option**            | Strict enforcement              | Small  |
| 24  | **Migrate CLI to cobra + viper**          | Extensibility, shell completion | Medium |
| 25  | **Syntax highlighting for code previews** | Professional terminal output    | Small  |

---

## g) Top #1 Question I Cannot Figure Out Myself

**Should the `Language` type live in `pkg/types` or in a new `pkg/language` package?**

- If it stays in `pkg/languages`, the cycle with `pkg/types` (`CodeBlock` needs `Language`, `languages` needs result types) persists.
- If it moves to `pkg/types`, then `pkg/types` becomes a kitchen-sink package and every validator implementation still has to import it.
- A new `pkg/language` package with _only_ the `Language` branded type and minimal constants would break the cycle cleanly, but it introduces a third package and may feel like overkill.

I can implement any of the three, but the choice has long-term consequences for the modularization proposal already in `docs/modularization/`. Which direction aligns with the intended module boundaries?
