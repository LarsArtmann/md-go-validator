# Status Report — 2026-07-07 01:32

**Scope:** `go-finding` v1.0.0 → v1.2.0 upgrade fallout + `nix build` reconciliation.
**Trigger:** `buildflow --fix --semantic --build-mode=full` failing on `nix-build` / `nix-build-verify` / `nix-hash-fix` after the dependency bump.
**Outcome:** All build paths green (`go build`, `go test`, `nix build`, `nix flake check`).

---

## TL;DR

A breaking API change in `go-finding` v1.2.0 (`Position.File` became a branded `FilePath` type) was fixed in the Go code **in turn 1**, but the **nix build kept failing in turn 2** because a flake input (`go-finding-src`) was still pinned to **v1.0.0** and a `postPatch` `replace` directive in `package.nix` silently overrode `go.mod`'s v1.2.0 with the v1.0.0 source at build time. The real fix required a **coordinated three-place update** that I performed in two disconnected steps.

---

## a) FULLY DONE ✅

| #   | Item                                                                                                                                   | Evidence                                                                                              |
| --- | -------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| 1   | Fixed `go-finding` v1.2.0 breaking API in `pkg/finding/finding.go:26` — wrapped `r.File.String()` with `finding.FilePath(...)`         | `go build ./...` clean; `go test ./...` all packages `ok`                                             |
| 2   | Bumped `go-finding-src` flake input `v1.0.0` → `v1.2.0` in `flake.nix:20`                                                              | input ref now `refs/tags/v1.2.0`                                                                      |
| 3   | Re-locked `flake.lock` — `go-finding-src` → rev `52d4e9d`, tag `v1.2.0`, narHash `sha256-OU94sIP+feRDta+IF0fpBiHiqK2dR9qK8VPszRaFK3Y=` | `flake.lock` verified                                                                                 |
| 4   | Confirmed `vendorHash` unchanged (`sha256-vQQZ…`) via `lib.fakeHash` probe + successful build — no hash drift                          | `nix build .#` exit 0                                                                                 |
| 5   | End-to-end verification                                                                                                                | `go build` ✅ · `go test ./...` ✅ · `nix build .#` ✅ · `nix flake check` → **all checks passed** ✅ |

---

## b) PARTIALLY DONE 🟡

1. **Dependency upgrade is complete but uncommitted.** `go.mod`/`go.sum` bump `go-finding` v1.0.0→v1.2.0 **and** `gotreesitter` v0.20.9→v0.21.0. These were pre-existing modified files at session start (`M go.mod`, `M go.sum`, `M flake.lock`, `M package.nix`); I only added the `go-finding-src` lock + flake.nix + finding.go changes on top. Nothing has been committed.
2. **`buildflow` full run not re-verified by me.** `nix flake check` (which buildflow's `nix-build` / `nix-flake-check` / `nix-build-verify` map to) passes, so the 3 previously-failing steps should now pass — but buildflow also runs independent steps (`jscpd`, `golangci-lint`, `govalid-generate`, `todo-check`) I did not re-execute.

---

## c) NOT STARTED ⬜

- Re-running the full `buildflow` suite to confirm **37/37** steps pass (user only pasted logs; I did not invoke `buildflow`).
- Running `golangci-lint run ./...` as a standalone check (flake check covers build+test+format, **not** the golangci-lint step).
- Updating `AGENTS.md` with the `go-finding-src` replace-directive gotcha (see §e).
- Committing any of this work.

---

## d) TOTALLY FUCKED UP 💥

**Turn 1 declared "done" prematurely — I created a build-path split-brain.**

In turn 1 I:

1. Saw the typecheck error (`cannot use r.File.String() as FilePath`).
2. Patched `pkg/finding/finding.go`.
3. Ran only `go build ./...` and `go test ./...`.
4. Saw green and said _"Build and tests pass."_

What I **missed**: the nix build was **never run**, and it is the **authoritative** build path for this project. Because `package.nix` injects a `replace github.com/larsartmann/go-finding => <flake-input-src>` via `postPatch`, the nix build compiles against **whatever version the `go-finding-src` flake input points to** — NOT the version in `go.mod`. The flake input was still pinned to **v1.0.0**, whose `Position.File` is a plain `string`. So:

- Turn 1 `go build` (uses go.mod v1.2.0): the `finding.FilePath(...)` cast was **correct** → green.
- Turn 2 `nix build` (uses flake input v1.0.0 via replace): the same cast was **wrong** — _"cannot use FilePath as string"_ → red.

I fixed a symptom in turn 1, broke the nix build in doing so, and only noticed in turn 2 when the user re-ran buildflow. The **single correct fix** was a coordinated update of **Go code + flake input + flake.lock together**, which I did across two turns because I never read `flake.nix`/`package.nix` and never ran `nix build` in turn 1.

**Root behavioral error:** I used the non-authoritative build path and skipped the project's canonical `nix build` / `nix flake check` — despite `AGENTS.md` listing them as the primary commands.

---

## e) WHAT WE SHOULD IMPROVE 🛠️

### Process

1. **Always run `nix build` / `nix flake check`, not just `go build`.** This project's source of truth is the nix build (the `postPatch` replace proves it). `go build` alone is insufficient and can give false green.
2. **Detect version skew proactively.** When `go.mod` pins a dependency at vX and a flake input pins the same dependency at vY with a `replace` override, that is a **latent bug**. The `go-finding-src` input must stay in lockstep with `go.mod`'s `go-finding` line — or the replace mechanism should be removed.
3. **Read `flake.nix` + `package.nix` before editing Go that touches a replace-overridden dependency.** The replace directive means the Go API you compile against locally may differ from what nix compiles against.
4. **Document the replace mechanism in `AGENTS.md`.** This is a non-obvious, hard-to-discover gotcha exactly the kind of thing AGENTS.md exists for. It belongs under a "Nix Gotchas" subsection.

### Architecture

5. **Reconsider the `postPatch` replace for `go-finding`.** `go-finding` v1.2.0 is a published, tagged, proxy-available module. A `replace` directive forcing every bump to be a 3-place update (go.mod + flake input + flake.lock) is friction that **just caused this bug**. If the replace exists only for pre-publish local iteration, it should be dropped now that v1.2.0 is on the proxy. _(See §g.)_
6. **Single source of truth for dependency versions.** Today: `go.mod` says v1.2.0, flake input says (was) v1.0.0. Make one canonical; ideally just `go.mod`.

### Understanding gap to close

7. **The `vendorHash` did not change** between v1.0.0 and v1.2.0 replace targets. My hypothesis: with `proxyVendor = true`, the `go-modules` FOD hashes only **proxy-fetched** deps, and the replace target (a nix store path) is injected at the **build** step, not counted in the FOD hash. I have not rigorously confirmed this. Worth verifying so future bumpers aren't confused when the hash stays stable across a version that clearly changed source.

---

## f) Top 25 — What to get done next

> Sorted by rough impact. Strike-through = already handled this session.

1. ~~Fix `go-finding` v1.2.0 API break in `pkg/finding/finding.go`~~ ✅
2. ~~Sync `go-finding-src` flake input → v1.2.0 + re-lock~~ ✅
3. **Commit** the coordinated dependency bump (`go.mod`, `go.sum`, `flake.nix`, `flake.lock`, `finding.go`) with a message explaining the replace-mechanism coupling.
4. Re-run **full `buildflow`** and confirm 37/37 steps pass (esp. `jscpd`, `golangci-lint`, `govalid-generate`, `todo-check`).
5. **Decide on the `postPatch` replace directive** for `go-finding` — keep or remove? _(See §g.)_
6. Update **`AGENTS.md`** "Nix" section: document that `go-finding-src` flake input + `postPatch` replace override `go.mod`, and that both must move together on every `go-finding` bump.
7. Add a **drift guard**: a CI/dev check that asserts `go.mod`'s `go-finding` version == the `go-finding-src` flake input tag. Prevents this entire class of bug.
8. Verify the **`vendorHash` stability hypothesis** (§e #7) — confirm proxyVendor behavior so the hash non-change is understood, not lucky.
9. Clear the **stale LSP typecheck diagnostic** still showing on `finding.go:26` (the file is fixed; `golangci_lint_ls` cache is stale — restart/re-scan).
10. Run `golangci-lint run ./...` standalone to confirm 0 issues (independent of flake check).
11. Review whether `gotreesitter` v0.20.9→v0.21.0 needs any code-side changes (build is green, but confirm no deprecated APIs silently used).
12. Add/expand an **integration test** that exercises the `finding.FromResult` → `go-finding` round-trip against the **branded** `FilePath` type, so future API breaks fail in tests not in CI builds.
13. Consider a **`make`/flake `check` alias** that runs `go build && nix build && go test && golangci-lint` in one command — removes the temptation to stop at `go build`.
14. Audit **other flake inputs** for version skew vs `go.mod` (only `go-finding-src` has a replace today, but patterns drift).
15. Bump coverage on `pkg/finding` (currently 100% per AGENTS.md — keep it; ensure the new `FilePath` cast path is covered).
16. Add a **pre-commit/CI guard** that `nix flake check` must pass before merge (if not already enforced).
17. Sweep `flake.nix` inputs for `follows` correctness (go-finding-src is `flake=false`, fine; verify nixpkgs follow chain is intact after re-lock).
18. Regenerate/refresh **`FEATURES.md`** if the go-finding v1.2.0 branded types change any documented output behavior.
19. Tag the next release once committed (project uses `goreleaser`; version derives from git rev).
20. Backfill a one-line note in `package.nix` (comment) explaining **why** the `postPatch` replace exists, so future readers don't delete it unknowingly.
21. Consider switching `go-finding-src` from `git+ssh://` to the **Go module proxy** as the flake input source (ssh requires deploy keys in CI).
22. Run `nix flake update --all` selectively to refresh other stale inputs (nixpkgs, treefmt-nix) if desired — **only after** confirming this fix is stable.
23. Double-check the **overlay** (`flake.overlays.default`) still builds — it calls `package.nix` **without** `go-finding-src`, so the replace is skipped there; confirm that path is intended and green.
24. Confirm `nix flake check --all-systems` passes (cross-platform: aarch64-darwin/linux, x86_64-darwin) — this run only checked x86_64-linux.
25. Update **`TODO_LIST.md`** with any of the above that survive triage as real work items.

---

## g) Top #1 Question I Cannot Answer Myself ❓

**Why does `package.nix` use a `postPatch` `replace` directive to override `go-finding` with the `go-finding-src` flake input — and should it be removed now that v1.2.0 is published on the Go proxy?**

`go-finding` v1.2.0 is a tagged, proxy-available module (`go.mod` references it directly and `go build` resolves it fine). Yet `package.nix` appends `replace github.com/larsartmann/go-finding => <flake-input-store-path>`, which means:

- The nix build **ignores** `go.mod`'s `go-finding` version.
- Every `go-finding` bump requires updating **three** places in lockstep (`go.mod`, `flake.nix` input, `flake.lock`), and forgetting any one of them produces exactly the split-brain failure we just spent two turns debugging.

I cannot tell whether this `replace` is:

- **(A) Intentional and still needed** — e.g., you iterate on `go-finding` locally and publish _after_ validating against `md-go-validator`, so the replace lets you test unpublished source; **or**
- **(B) Legacy** — left over from before v1.2.0 was published, now pure overhead and a bug magnet.

If **(B)**, I recommend deleting the `postPatch` block, the `go-finding-src` input, and the `package.nix` `go-finding-src` parameter — collapsing to a single source of truth (`go.mod`). If **(A)**, I recommend at minimum adding the drift guard (item #7) and documenting the coupling in `AGENTS.md` (item #6).

This is a design decision only you can make.
